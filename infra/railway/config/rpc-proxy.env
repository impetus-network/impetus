# rpc-proxy — Caddy reverse proxy (image tranhuyduan/impetus-rpc-proxy)
# service id ee2c1079-ec1f-4dda-a011-29f99a0cbda7
# Public endpoint: https://rpc-proxy-production-a44c.up.railway.app
# Enforces Origin/Referer must match *.impetus.network + per-IP rate limit,
# then proxies HTTP+WS to the upstream RPC node. Config in ../rpc-proxy/Caddyfile.
UPSTREAM=rpc-singapore.railway.internal:9944
