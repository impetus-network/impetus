# Impetus mainnet (chain id 388266). Point at a node RPC the indexer can reach
# server-side (e.g. the archive service's internal URL) — NOT the browser
# rpc-proxy (it 403s non-impetus.network Origins and is pruned).
PONDER_RPC_URL_388266=http://127.0.0.1:9944
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/impetus_indexer
