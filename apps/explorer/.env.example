# Postgres written by apps/substrate-indexer (the Subsquid squid). It populates
# the four Subscan-shape tables this explorer reads: evm_blocks,
# evm_transactions, balance_accounts, evm_contracts. Use a read-only role in prod.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/impetus_squid
NEXT_PUBLIC_CHAIN_ID=388266
NEXT_PUBLIC_CHAIN_NAME=Impetus
NEXT_PUBLIC_TOKEN_SYMBOL=IPT
NEXT_PUBLIC_TOKEN_DECIMALS=18
