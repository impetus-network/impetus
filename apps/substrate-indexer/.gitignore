node_modules
lib
.env
versions.jsonl
# Regenerated by `sqd codegen` in the Docker build:
src/model/generated
# src/types (typegen output) IS committed so the image builds from git without
# needing the chain RPC at build time. Regenerate with `npm run metadata && typegen`.
