{
  "name": "@artemis/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "build": "hardhat compile",
    "test": "hardhat test --network impetus_dev",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@artemis/shared": "workspace:*"
  },
  "devDependencies": {
    "@chainlink/contracts": "^1.5.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "@polkadot/api": "^16.5.6",
    "hardhat": "^2.22.0",
    "typescript": "^5.5.0",
    "viem": "^2.21.0"
  }
}
