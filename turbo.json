{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "artifacts/**", "typechain-types/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "copy-abi": {
      "dependsOn": ["^build"],
      "outputs": ["abis/**"]
    },
    "lint": {},
    "node:build": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
